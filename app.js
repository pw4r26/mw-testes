const seed=[{id:'ST-2026-001',name:'Trabalhador Exemplo A',job:'Operador de produção',status:'Em análise',tag:''},{id:'ST-2026-002',name:'Trabalhadora Exemplo B',job:'Auxiliar administrativa',status:'Aguardando documentos',tag:'blue'},{id:'ST-2026-003',name:'Trabalhador Exemplo C',job:'Motorista',status:'Finalizado',tag:'green'}];let cases=[...seed];const list=document.querySelector('#cases'),search=document.querySelector('#search'),modal=document.querySelector('#modal');function render(q=''){const f=cases.filter(x=>(x.name+x.id+x.job).toLowerCase().includes(q.toLowerCase()));list.innerHTML=f.map(x=>`<article class="case"><div><b>${x.name}</b><small>${x.id}</small></div><div><b>${x.job}</b><small>Saúde do Trabalhador</small></div><div><span class="tag ${x.tag}">${x.status}</span></div><button onclick="openCase('${x.id}')">Abrir caso</button></article>`).join('')||'<p>Nenhum caso encontrado.</p>'}function openCase(id){location.href=`case.html?id=${encodeURIComponent(id)}`}document.querySelector('#newWorker').onclick=()=>modal.showModal();document.querySelector('#newCase').onclick=()=>modal.showModal();search.oninput=e=>render(e.target.value);document.querySelector('#saveWorker').onclick=e=>{const name=document.querySelector('#name').value.trim(),job=document.querySelector('#job').value.trim();if(!name){e.preventDefault();return}cases.unshift({id:`ST-2026-${String(cases.length+1).padStart(3,'0')}`,name,job:job||'Ocupação não informada',status:'Rascunho',tag:'blue'});render();};render();